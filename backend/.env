# Twilio Configuration (Optional)
# If these are provided, the backend will send real SMS messages instead of simulating them in the terminal.
# Sign up at https://www.twilio.com/ to get these credentials.

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
